[package]
name = "gitlab-connect-infra"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "GitLab vendor HTTP adapter for GitLab Connect"
license.workspace = true

[lints]
workspace = true

[dependencies]
gitlab-connect-configuration = { path = "../configuration" }
gitlab-connect-domain = { path = "../domain" }
idp-connect-contract = { path = "../../idp-connect-contract" }

async-trait = { workspace = true }
oauth2 = { workspace = true }
reqwest = { version = "0.11", features = ["json"] }
serde = { workspace = true }
tracing = { workspace = true }
