kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: apparatus-p4-it
networking:
  # kindnet n'enforce pas NetworkPolicy. Calico piné dans KindCluster::ensure.
  disableDefaultCNI: true
  podSubnet: "192.168.0.0/16"
nodes:
  - role: control-plane
