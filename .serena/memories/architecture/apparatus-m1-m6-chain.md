# Apparatus M1–M6 chain

Décision utilisateur 2026-09-22 : jalons séquentiels du contrat `docs/apparatus-0002-0008-next-milestones.md`. Un jalon à la fois. Pas de commit. Pas de flip Réalité. Pas d’ADR-0009. Pas de SuperSède. Ne pas relivrer T1 / T2–T12.

- **M1 VERT (2026-09-22)** : `m1_reference_kv_pinned_envelope_on_zot` (`apparatus-operator/tests/apparatus_m1_reference_kv_pin.rs`). Identité catalogue = digest descripteur 0002 (`descriptor_from_disk` / `apparatus-reference-kv/apparatus.toml`). CRI ≠ enveloppe OCI. 0002/0008 restent Partial.
- **M2 VERT (2026-09-22)** : `m2_admit_get_returns_valid_for_m1_digest` (`apparatus-operator/tests/apparatus_m2_valid_persist.rs`, 8 tests). Store produit = JSON `PersistentAdmissionStore`, path **obligatoire** `APPARATUS_ADMISSION_STORE_PATH` (TCB, pas de défaut temp). Adm-A seule écriture VALID. CR Kind ≠ store produit. 0005/0008 restent Partial.
- **M3 VERT (2026-09-22)** : `m3_kind_cr_only_valid_does_not_schedule` (`apparatus-operator/tests/apparatus_m3_install_fail_closed.rs`, 6 tests). Gate JSON avant schedule. CR VALID sans JSON → `MissingAdmissionRecord`, zéro Pod. Pin CRI = `bind_cri` JSON, pas `spec.criImage` CR (S-1 levé). Isolation toujours sur labels CR. 0005/0008 restent Partial.
- **M4 VERT (2026-09-22)** : `m4_ready_valid_digest_schedules` (`apparatus-operator/tests/apparatus_m4_desired_state_bridge.rs`, 3 tests). Observer HTTP hors Manifesto src : GET list des 5 routes `/components`. Ready = `status==active` + digest `sha256:` 64 hex. VALID JSON → Pod piné M3 ; non VALID → zéro Pod. Ticker P2 inchangé. 0006 G/E tenus. 0001/0006/0008 non flippés (0008 reste Partial).
- **M5 VERT (2026-09-22)** : `m5_invoke_reaches_isolated_plugin_pod` (`apparatus-operator/tests/apparatus_m5_invoke_isolated.rs`) vert. Hop Lazaret `forward_invoke` POST `INVOKE_PATH` (locator injecté, pas de kube dans Lazaret). Deny 403 sans hop ; allow 200 hostname=pod. T11 canary bloqué dans le même test. 0003/0008 restent Partial ; 0004/0007 Implemented. G/E tenus.
- **M6 VERT (2026-09-22)** : `m6_e2e_0002_0008_chain` (`apparatus-operator/tests/apparatus_m6_e2e_chain.rs`) vert, 1/1 en 271.52 s, exit 0. Helpers partagés : `apparatus-operator/tests/fixtures/chain.rs` (M6 seulement, M1–M5 non churn). Un scénario Kind : `SignRegistryStack` + `push_and_sign_envelope` (pin zot que M5 sautait) → JSON VALID `PersistentAdmissionStore` → `HttpComponentsClient` GET `/components` → schedule plugin `apparatus-plugins` → Lazaret `InvokeService` locator injecté POST `INVOKE_PATH` → T11 wget canary bloqué. **Identité = digest catalogue 0002** (`descriptor_from_disk` / `digest_manifest` de `apparatus-reference-kv/apparatus.toml`) : `sha256:98ba747fc572de29d76dfd08f92537782bf0353b652adcace10200020ee560cf`. CRI `@sha256` ≠ enveloppe OCI ≠ catalogue. Réalité ADR **inchangée** (0008 reste Partial ; aucun flip 0002–0008). Pas d’ADR-0009. Pas de `Factory/`. Pas de kube client Lazaret. Pas de 6e route Manifesto.
- Chaîne M1–M6 livrée comme preuves IT. Flips Réalité = section (B) du contrat, accord humain requis. 0008 reste Partial (TCB Transit hors `-dev`, Adm-B, cluster prod).
- Résiduels : HMAC/Transit JSON hors M3 (TCB fichier) ; T5 binaire admit `conformance_passed: true` (préexistant T10) ; S-4 pod-exists freeze.
