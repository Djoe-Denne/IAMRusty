apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - lazaret-invoke-configmap.yaml
  - invoke-probe-job.yaml
patches:
  - path: patch-lazaret-http.yaml
  - path: patch-plugin-http.yaml
# HelmRelease Flux : helmrelease-envoy-gateway.yaml (commenté, crd-less).
# Ne pas l'ajouter à resources — Flux n'est pas installé sur aiforall-local.
