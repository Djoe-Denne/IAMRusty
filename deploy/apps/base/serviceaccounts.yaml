# SA 0601 : gateway sur Lazaret. Plugins : pas de SA privilégié (token non monté).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gateway
  namespace: aiforall-gateway
  labels:
    app.kubernetes.io/name: lazaret
    app.kubernetes.io/part-of: aiforall
automountServiceAccountToken: false
